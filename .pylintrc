[MASTER]
# Ensure pylint can resolve imports like `from gui.app import ...`.
init-hook='import sys; sys.path.insert(0, "src")'

[MESSAGES CONTROL]
# This repo intentionally keeps some large modules while refactoring.
# Prefer local disables for more granular control when practical.

